import 'package:flutter/material.dart';

Widget goodTextStyle(String data) => Text(
      data,
      style: const TextStyle(
        fontWeight: FontWeight.bold,
        fontSize: 20,
        color: Colors.black,
      ),
    );
